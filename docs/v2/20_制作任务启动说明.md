# v2 制作任务启动说明

## 当前状态

- 旧 WASD Demo 已弃用并移出项目；新代码直接写入干净的 `src/`。
- `docs/v2/` 是唯一设计规范来源；其他旧版报告不参与实现。
- 巫通儿模型与 Blender 工程保存在 `source-assets/character/wutonger/`，不直接进入网页构建。
- v2 风格探索图保存在 `references/visual/explorations/`，用于构图与材质校准。
- 项目已配置 React、Three.js、React Three Fiber、HashRouter、ESLint、Prettier 与 Playwright；依赖尚待安装。

## 新任务首轮目标

只完成“工程基底 + 杨家堂纵向切片”的第一小步，不同时铺开七个观察站：

1. 安装并验证依赖、lint、build 与 Playwright 浏览器。
2. 阅读并复述核心约束，检查规范之间是否冲突。
3. 建立数据模型、总览路由、村落路由、观察站路由和数字标本路由。
4. 建立统一的小队占位形象、来源状态、授权状态、公开范围过滤。
5. 先实现档案总览与杨家堂章节骨架，再进入白猫门洞场景。

## 新对话启动文本

```text
请开始《巫通儿：松阳寻踪》v2 正式制作。

项目路径：D:\codex_sy\wutonger-sunyang

旧 Demo 已弃用，不要从 D:\codex_sy\_discarded 中恢复任何旧代码。
先完整阅读 README.md、docs/v2/00、01、03、04、05、07、08、09、10、13、14、18、20。
以 docs/v2 为唯一设计依据，reference 和 source-assets 只作制作输入。

第一阶段只做：
1. 安装并验证项目依赖；
2. 建立数据驱动的 v2 工程基底；
3. 实现档案总览、三村入口和自由导航；
4. 开始杨家堂纵向切片，不同时制作其余六站；
5. 所有文字必须来自数据文件，不把待核验内容写成事实。

先检查环境和规范冲突，给出简短执行计划，然后直接实施、测试并启动本地服务器。
```

## 开工判定

设计与信息架构已足够开始原型。下列内容不阻塞工程基底，但阻塞对应正式内容或公开发布：

- 其他成员尚未提交的田野旁注。
- 松庄展板逐字转写。
- 四件数字标本的最终模型。
- 音频候选文件及许可证存档。
- 每张实拍和人物/作品的最终授权台账。

开发时用数据占位或隐藏状态处理，不使用 AI 自动补写正式正文。
